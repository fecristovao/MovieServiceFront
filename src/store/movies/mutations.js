export const setServices = (state, service) => {
    state.services = service
}
export const setMovies = (state, movies) => {
    state.foundMovies = movies
}

export const setAvailableServices = (state, services) => {
    state.availableServices = services
}
